export interface CollectionAsset {
  collectionId: string;
  assetId: string;
  order: number;
}

export interface CollectionAssetTable {
  collection_id: string;
  asset_id: string;
  order: number;
}
